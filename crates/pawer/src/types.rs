/// Default floating-point type for all computations in the `pawer` crate.
///
/// Defaults to [`f32`]. Switch to [`f64`] by changing this alias if
/// double-precision is required.
pub type Real = f32;
